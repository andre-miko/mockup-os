# Success criteria

- A returning user can check balances and open one account in ≤3 taps.
- A user can complete a transfer between their own accounts in ≤4 taps.
- No approved screen ships without at least populated + empty states.
- Every interactive mockup passes `npm run validate` with zero errors.
